# Entrega F03-T04 — Testar reinício e preparar migrações (Gate F03)

## Identificação

- ID e título: F03-T04 — Testar reinício e preparar migrações
- Data e responsável: 2026-09-13, Antigravity (autorizado por Rafael)
- Estado: CONCLUÍDA
- Tipo de evidência: local / emulador real (Pixel 10 Pro XL) e testes instrumentados
- HEAD base e alterações não commitadas: `cd49296` + código F01, F02 e F03
- Pedido autorizado e dependências verificadas: F03-T01, F03-T02 e F03-T03 concluídas com sucesso; Gate F03 satisfeito

## Mudança e aceite

- Problema e comportamento resultante: Comprovação da persistência durável dos dados no Room SQLite através do ciclo de vida completo do processo (fechamento de conexão e processo Android), validação de integridade referencial com cascata, atomicidade de transações com rollback em erro e política rigorosa de migração de banco de dados sem permissão de reset destrutivo automático.
- Arquivos criados/alterados e motivo:
  - `app/src/androidTest/java/com/playertwo1/rin/DatabaseRestartTest.kt` [NEW]: Teste instrumentado comprovando sobrevida dos dados no arquivo de banco em disco após fechamento forçado da conexão (`db.close()`) e reabertura em nova instância.
  - `docs/development/SETUP.md` [MODIFY]: Registro da política de migração de schema do Room, proibição de migração destrutiva e diretrizes para testes futuros com `MigrationTestHelper`.
  - `docs/evidence/screenshots/F03-projects-restart-offline.png` [NEW]: Captura de tela no emulador Pixel 10 Pro XL comprovando recuperação dos dados salvos no SQLite após encerramento forçado do processo (`adb shell am force-stop`) e reabertura offline.
  - `docs/evidence/F03-T04-2026-09-13.md` [NEW]: Evidência formal do gate de saída da Fase F03.
- Critérios da tarefa, resultado de cada um e evidência:
  1. Testar fechar conexão, recriar processo e reler banco: ATENDIDO. Teste instrumentado `DatabaseRestartTest.database_persists_data_across_close_and_reopen_on_disk` aprovado e teste manual de `am force-stop` com captura de tela no emulador aprovados.
  2. Versionar schema inicial: ATENDIDO. Arquivo versionado em `app/schemas/com.playertwo1.rin.core.database.RinDatabase/1.json`.
  3. Proibir fallback destrutivo silencioso: ATENDIDO. `fallbackToDestructiveMigration` expressamente proibido no código e registrado em `ADR 0003` e `SETUP.md`.
  4. Não alegar teste de migração inexistente: ATENDIDO. Registrado com honestidade técnica que como o banco está na versão inicial 1, migrações só serão implementadas e testadas a partir de versões subsequentes reais.
  5. Dados sobrevivem a reinício sem reset automático: ATENDIDO. Todos os 3 projetos e seus checkpoints foram reexibidos intactos.
- Decisões/ADRs afetadas: Segue D007, D009, D018 e [ADR 0003](../adr/0003-modelo-local-e-persistencia.md).
- Alterações anteriores do usuário preservadas: Sim.

## Avaliação do Gate de Saída da Fase F03

| Critério do Gate de Saída | Verificação | Evidência |
|---|---|---|
| **Persistência durável comprovada** | Dados sobrevivem a fechamento de conexão de banco e morte do processo do app | Teste `DatabaseRestartTest` e screenshot `F03-projects-restart-offline.png` |
| **Identidades e autoridade dos dados** | IDs locais estáveis (UUID), desacoplamento de status de negócio e sync, e isolamento de origens remotas sem colisão | Testes em `IdentityAndInvariantsTest` e `RoomPersistenceTest` |
| **Integridade e atomicidade** | Rollback atômico em falha sem resíduos parciais; deleção em cascata preservando integridade referencial | Testes `transaction_failure_does_not_leave_partial_records` e `deleting_project_cascades_and_deletes_associated_checkpoints` |
| **Fluxo offline-first demonstrado** | UI observa exclusivamente o Room via Flow; refresh grava no SQLite antes da emissão; offline preserva cache histórico sem marcar como fresco | Testes em `WorkstationSyncManagerTest` e telas no emulador |
| **Schema versionado e proteção contra perda** | Schema JSON v1 exportado; proibição total de reset destrutivo | `app/schemas/.../1.json` e documentação em `SETUP.md` |

**GATE F03 RESULTADO: SATISFEITO (Aprovado para início de F04 — Projetos locais e Home).**

## Validação executada

| Comando ou roteiro exato | Ambiente/revisão | Exit code/resultado observado | Artefato existente |
|---|---|---|---|
| `.\gradlew.bat testDebugUnitTest` | Local / JVM | Exit 0; 21 testes unitários aprovados | Relatório Gradle |
| `.\gradlew.bat connectedDebugAndroidTest` | Emulador Pixel 10 Pro XL (Android 17 / API 37) | Exit 0; 6 testes instrumentados aprovados | Relatório androidTest Gradle |
| `.\gradlew.bat lintDebug` | Local / Windows | Exit 0; 0 erros | Relatório HTML |
| `.\gradlew.bat assembleDebug` | Local / Windows | Exit 0; APK gerado | `app-debug.apk` |
| Encerramento de processo e reabertura | `adb shell am force-stop` + `am start` | Exit 0; dados recuperados do SQLite | Screenshot `F03-projects-restart-offline.png` |

- Testes não executados e motivo: Migração entre versões de banco NÃO EXECUTADA (banco inicial na v1).
- Limites desta evidência: A persistência é comprovada localmente no dispositivo físico emulado; a conectividade real via rede LAN pertence à Fase F08.

## Auditoria

- Tipo: autoauditoria segundo AUDIT.md e WATCHDOG.md
- Escopo, diff, regressão, qualidade, segurança, Git e caça a atalhos:
  - Zero atalhos; todas as 4 tarefas da fase F03 foram executadas estritamente e com validações reais.
  - Nenhuma perda de dados permitida; atomicidade transacional e rollback garantidos.
  - Sem dados sensíveis ou segredos.
- AUDIT RESULT: PASS
- Achados: Nenhum.
- Riscos residuais: Nenhum para a Fase F04.

## Continuidade

- PROJECT_STATE e CHANGELOG atualizados: Sim.
- Gate de fase: F03 CONCLUÍDA.
- Próximo ID elegível: **F04-T01** — Especificar fluxo de criação e listagem local (Fase F04).
- Bloqueios: Nenhum.
