# Entrega F05-T01 — Criar registro de checkpoint

## Identificação

- ID e título: F05-T01 — Criar registro de checkpoint
- Data e responsável: 2026-09-13, Antigravity (autorizado por Rafael)
- Estado: CONCLUÍDA
- Tipo de evidência: local / emulador real (Pixel 10 Pro XL) e testes automatizados
- HEAD base e alterações não commitadas: `cd49296` + F01, F02, F03, F04
- Pedido autorizado e dependências verificadas: F04 concluída; continuidade autônoma autorizada por Rafael ("avance toda as fases que puder que nao precisar da minha intervenção")

## Mudança e aceite

- Problema e comportamento resultante:
  - Criação da interface e lógica de persistência para checkpoints manuais no projeto, com resumo factual obrigatório, próximo passo imediato, bloqueios ou impedimentos e referências opcionais.
  - Ordenação cronológica estável decrescente (`createdAt DESC`) no Room SQLite.
  - Preservação do timestamp `createdAt` original na edição e geração de UUID estável na criação.
  - Exibição de histórico de checkpoints na tela de detalhe do projeto, com estado vazio educativo orientando o usuário a registrar seu primeiro marco factual.
  - Ações de edição e exclusão de checkpoints individuais com confirmação e deleção em cascata garantida por chave estrangeira.
- Arquivos criados/alterados e motivo:
  - `app/src/main/java/com/playertwo1/rin/core/database/dao/CheckpointDao.kt` [MODIFY]: Adicionada query de exclusão por ID (`deleteCheckpointById`).
  - `app/src/main/java/com/playertwo1/rin/core/database/repository/ProjectRepository.kt` [MODIFY]: Métodos `getCheckpoint(checkpointId)` e `deleteCheckpoint(checkpointId)` adicionados ao contrato do repositório.
  - `app/src/main/java/com/playertwo1/rin/core/database/repository/RoomProjectRepository.kt` [MODIFY]: Implementação de `getCheckpoint` e `deleteCheckpoint` usando o DAO do Room.
  - `app/src/main/res/values/strings.xml` [MODIFY]: Adição de 18 strings e mensagens de validação e acessibilidade para o módulo de checkpoints.
  - `app/src/main/java/com/playertwo1/rin/ui/screens/projects/checkpoints/CheckpointFormViewModel.kt` [NEW]: ViewModel do formulário de checkpoint, contendo validação de campos obrigatórios, preservação de `createdAt` na edição, persistência no Room e atualização do `updatedAt` do projeto.
  - `app/src/main/java/com/playertwo1/rin/ui/screens/projects/checkpoints/CheckpointFormDialog.kt` [NEW]: Diálogo modal Compose (Material 3) com formulário de criação/edição de checkpoint, tratamento de erro visual e suporte a teclado virtual.
  - `app/src/main/java/com/playertwo1/rin/ui/screens/projects/ProjectDetailScreen.kt` [MODIFY]: Inclusão da seção "Histórico de Checkpoints", card educativo para estado vazio, cards individuais com badge de origem, data/hora e botões de editar e excluir com diálogo de confirmação.
  - `app/src/main/java/com/playertwo1/rin/ui/screens/projects/ProjectsViewModel.kt` [MODIFY]: Observação reativa de `observeCheckpoints(projectId)`, integração com `deleteCheckpointLocally` e estado de checkpoints por projeto.
  - `app/src/main/java/com/playertwo1/rin/ui/screens/projects/ProjectsScreen.kt` [MODIFY]: Conexão do `CheckpointFormViewModel`, `CheckpointFormDialog` e ações de criação, edição e exclusão de checkpoints no detalhe do projeto.
  - `app/src/test/java/com/playertwo1/rin/CheckpointFormViewModelTest.kt` [NEW]: 7 testes unitários cobrindo abertura para criação, abertura para edição, validação de resumo factual obrigatório, preservação de timestamp `createdAt`, atualização de `updatedAt` do projeto e cancelamento.
  - `app/src/androidTest/java/com/playertwo1/rin/CheckpointHistoryCrudTest.kt` [NEW]: 5 testes instrumentados cobrindo inserção, persistência e ordenação cronológica reversa, atualização com preservação de timestamp, exclusão individual e exclusão em cascata pela chave estrangeira com o projeto.
  - `docs/evidence/screenshots/F05-checkpoint-empty.png` [NEW]: Captura da tela de detalhe com estado vazio educativo e botão de adicionar primeiro checkpoint.
  - `docs/evidence/screenshots/F05-checkpoint-form-dialog.png` [NEW]: Captura do diálogo modal de criação de checkpoint.
  - `docs/evidence/screenshots/F05-checkpoint-validation-error.png` [NEW]: Captura da validação em vermelho com a mensagem "O resumo factual é obrigatório".
  - `docs/evidence/screenshots/F05-checkpoint-form-filled.png` [NEW]: Captura do formulário preenchido com fatos reais antes da persistência.
- Critérios da tarefa, resultado de cada um e evidência:
  1. Formulário com resumo factual obrigatório, próximo passo, bloqueios e referências: ATENDIDO. Implementado em `CheckpointFormDialog` e validado por testes unitários e na UI.
  2. Persistir e ordenar com IDs/timestamps estáveis: ATENDIDO. Chave primária UUID, ordenação `createdAt DESC` no Room e preservação de timestamps na edição.
  3. Histórico de checkpoints no detalhe do projeto: ATENDIDO. Renderizado na seção de checkpoints com contador e cards individuais formatados.
  4. Estado vazio educativo explicando como registrar o primeiro: ATENDIDO. Exibido com ícone educativo, texto de orientação e botão de ação primária.
- Decisões/ADRs afetadas: [ADR 0003](../adr/0003-modelo-local-e-persistencia.md), D007, D009, D012, D018.
- Alterações anteriores do usuário preservadas: Sim.

## Validação executada

| Comando ou roteiro exato | Ambiente/revisão | Exit code/resultado observado | Artefato existente |
|---|---|---|---|
| `.\gradlew.bat testDebugUnitTest` | Local / JVM | Exit 0; 45 testes unitários aprovados (7 novos de checkpoints) | Relatório de testes Gradle |
| `.\gradlew.bat connectedDebugAndroidTest` | Emulador Pixel 10 Pro XL (Android 17 / API 37) | Exit 0; 18 testes instrumentados aprovados (5 novos em CheckpointHistoryCrudTest) | Relatório androidTest Gradle |
| `.\gradlew.bat lintDebug` | Local / Windows | Exit 0; 0 erros de lint | Relatório HTML |
| `.\gradlew.bat installDebug` | Emulador Pixel 10 Pro XL | Exit 0; APK instalado com sucesso | `app-debug.apk` |
| Roteiro de validação visual via `adb` e screencap | Emulador Pixel 10 Pro XL | Exit 0; Diálogo, validação e estado vazio capturados | Screenshots em `docs/evidence/screenshots/` |
| `pwsh -File docs/quality/CHECK_DOCUMENTATION.ps1` | Local / PowerShell 7 | Exit 0; 0 erros em 71 arquivos e 315 links | Saída JSON do verificador |

- Testes não executados e motivo: Nenhum.
- Limites desta evidência: Registro, edição, exclusão e histórico de checkpoints locais validados; o fluxo de registro e confirmação de decisões locais pertence a F05-T02.
- Falhas encontradas e correções: Nenhuma.

## Auditoria

- Tipo: autoauditoria segundo AUDIT.md e WATCHDOG.md
- Escopo, diff, regressão, qualidade, segurança, Git e caça a atalhos:
  - SQLite Room sem `fallbackToDestructiveMigration`.
  - Checkpoints com chave estrangeira em cascata (`onDelete = ForeignKey.CASCADE`) e índice para performance.
  - Zero dependência de LLM ou invenção de dados no cliente móvel.
  - Cobertura de testes unitários (45) e instrumentados reais (18) completa.
- AUDIT RESULT: PASS
- Achados: Nenhum.
- Riscos residuais: Nenhum para a continuidade em F05-T02.

## Continuidade

- Próxima tarefa: F05-T02 — Registrar decisões locais
- Bloqueios: Nenhum
