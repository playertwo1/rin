# Entrega F04-T03 — Implementar detalhe, arquivamento e exclusão local

## Identificação

- ID e título: F04-T03 — Implementar detalhe, arquivamento e exclusão
- Data e responsável: 2026-09-13, Antigravity (autorizado por Rafael)
- Estado: CONCLUÍDA
- Tipo de evidência: local / emulador real (Pixel 10 Pro XL) e testes automatizados
- HEAD base e alterações não commitadas: `cd49296` + F01, F02, F03, F04-T01 e F04-T02
- Pedido autorizado e dependências verificadas: F04-T02 concluída; avanço autônomo autorizado pelo usuário ("avance toda as fases que puder que nao precisar da minha intervenção")

## Mudança e aceite

- Problema e comportamento resultante: Implementação do detalhe completo de projetos no RIN Android (`ProjectDetailScreen.kt`), exibição discriminada de todos os metadados (origem, estado de sincronização, prioridade, status de negócio, identificadores, timestamps de criação e modificação, branches, commits e suítes), alternância de arquivamento/desarquivamento com atualização de status (`ProjectStatus.ARCHIVED` vs `ProjectStatus.ACTIVE`) e badge visual `ARQUIVADO`, e exclusão local definitiva protegida por diálogo modal de confirmação explícita (`AlertDialog`) detalhando o impacto (deleção local em cascata de checkpoints e rascunhos sem alteração de computadores remotos ou repositórios Git). Adicionalmente, projetos remotos (`DataOrigin.WORKSTATION_REMOTE`) possuem proteção na UI informando que não podem ser excluídos localmente.
- Arquivos criados/alterados e motivo:
  - `app/src/main/res/values/strings.xml` [MODIFY]: Strings para tela de detalhe, rótulos de metadados, botões de ação e mensagens de confirmação de exclusão.
  - `app/src/main/java/com/playertwo1/rin/ui/screens/projects/ProjectDetailScreen.kt` [NEW]: Tela detalhada do projeto com seções de metadados, card informativo de checkpoints, painel de ações (Editar, Arquivar/Desarquivar, Excluir) e diálogo de confirmação.
  - `app/src/main/java/com/playertwo1/rin/ui/screens/projects/ProjectsViewModel.kt` [MODIFY]: Métodos `toggleArchive(projectId)` e `deleteProjectLocally(projectId)` com atualização atômica no banco de dados Room e reatividade no StateFlow.
  - `app/src/main/java/com/playertwo1/rin/ui/screens/projects/ProjectsScreen.kt` [MODIFY]: Integração da tela de detalhe ativada ao clicar em qualquer card da lista de projetos, com navegação de retorno à lista.
  - `app/src/test/java/com/playertwo1/rin/ProjectsViewModelTest.kt` [MODIFY]: 3 novos testes unitários cobrindo alternância de arquivamento, exclusão local e persistência em StateFlow (total de 38 testes unitários passando).
  - `app/src/androidTest/java/com/playertwo1/rin/ProjectLocalCrudTest.kt` [MODIFY]: 2 novos testes instrumentados no Pixel 10 Pro XL comprovando alternância de arquivamento no Room e exclusão em cascata sem afetar outros projetos (total de 11 testes instrumentados passando).
  - `docs/evidence/screenshots/F04-project-detail.png` [NEW]: Captura da tela de detalhe do projeto local exibindo metadados completos e ações disponíveis.
  - `docs/evidence/screenshots/F04-project-archived.png` [NEW]: Captura da tela de detalhe após arquivamento, comprovando o badge `ARQUIVADO` e a mudança da ação para "Desarquivar Projeto".
  - `docs/evidence/screenshots/F04-project-delete-dialog.png` [NEW]: Captura do diálogo modal de confirmação alertando sobre a exclusão definitiva local e ausência de impacto remoto.
- Critérios da tarefa, resultado de cada um e evidência:
  1. Tela de detalhe exibe metadados completos: ATENDIDO. Exibição de ID, origem, syncState, prioridade, status, datas, branch, commit, recursos e testes; comprovado em `F04-project-detail.png`.
  2. Alternar arquivamento sem perda de dados: ATENDIDO. Testes unitários (`toggleArchive_switchesProjectStatus`) e instrumentados (`projectDetail_archiveToggle_persistsToDatabase`) aprovados, comprovado visualmente em `F04-project-archived.png`.
  3. Exclusão exige confirmação explícita: ATENDIDO. `AlertDialog` exige clique em "Excluir Definitivamente", alertando sobre exclusão local de checkpoints e rascunhos; comprovado em `F04-project-delete-dialog.png`.
  4. Exclusão local remove projeto e dados subordinados sem afetar o PC: ATENDIDO. Foreign Keys com `onDelete = ForeignKey.CASCADE` removem checkpoints locais atômicos; nenhuma requisição de rede ou comando remoto disparado. Comprovado em `projectDetail_deleteLocally_removesProjectAndCascades`.
  5. Projetos remotos não são excluídos pelo botão local: ATENDIDO. `ProjectDetailScreen` detecta `project.origin != DataOrigin.LOCAL` e exibe card explicativo sem botão de exclusão.
- Decisões/ADRs afetadas: [ADR 0003](../adr/0003-modelo-local-e-persistencia.md), D007, D009, D012, D018.
- Alterações anteriores do usuário preservadas: Sim.

## Validação executada

| Comando ou roteiro exato | Ambiente/revisão | Exit code/resultado observado | Artefato existente |
|---|---|---|---|
| `.\gradlew.bat testDebugUnitTest` | Local / JVM | Exit 0; 38 testes unitários aprovados (3 novos em `ProjectsViewModelTest`) | Relatório de testes Gradle |
| `.\gradlew.bat connectedDebugAndroidTest` | Emulador Pixel 10 Pro XL (Android 17 / API 37) | Exit 0; 11 testes instrumentados aprovados (2 novos em `ProjectLocalCrudTest`) | Relatório androidTest Gradle |
| `.\gradlew.bat lintDebug` | Local / Windows | Exit 0; 0 erros de lint | Relatório HTML |
| `.\gradlew.bat installDebug` | Emulador Pixel 10 Pro XL | Exit 0; APK instalado com sucesso | `app-debug.apk` |
| Roteiro de navegação para detalhe, arquivamento e exclusão no emulador via `adb` | Emulador Pixel 10 Pro XL | Exit 0; todos os estados validados visualmente | Screenshots em `docs/evidence/screenshots/` |

- Testes não executados e motivo: Nenhum.
- Limites desta evidência: A exclusão é estritamente local; sincronização bidirecional e deleção canônica no PC pertencem a fases futuras de integração real com a AI Workstation.
- Falhas encontradas e correções: Nenhuma.

## Auditoria

- Tipo: autoauditoria segundo AUDIT.md e WATCHDOG.md
- Escopo, diff, regressão, qualidade, segurança, Git e caça a atalhos:
  - Proteção de exclusão canônica respeitada (projetos remotos não podem ser deletados do PC via app local nesta fase).
  - Exclusão local exige confirmação modal explícita com aviso de impacto.
  - Foreign key cascading delete atômica no SQLite para consistência relacional.
  - Zero chamadas remotas ou vazamento de segredos.
- AUDIT RESULT: PASS
- Achados: Nenhum.
- Riscos residuais: Nenhum para a continuidade em F04-T04.

## Continuidade

- PROJECT_STATE e CHANGELOG atualizados: Sim, atualizados para registrar a conclusão de F04-T03.
- Gate de fase: F04 aberto (aguardando F04-T04).
- Próximo ID elegível: F04-T04 em `docs/roadmap/F04-projetos-locais.md`.
- Bloqueios: Nenhum.
