# Entrega F01-T01 — Criar build reproduzível

## Identificação

- ID e título: F01-T01 — Criar build reproduzível
- Data e responsável: 2026-09-13, Antigravity (autorizado por Rafael)
- Estado: CONCLUÍDA
- Tipo de evidência: local / compilação real
- HEAD base e alterações não commitadas: `cd49296` + arquivos de F00
- Pedido autorizado e dependências verificadas: Fase F01 autorizada; gate F00 satisfeito

## Mudança e aceite

- Problema e comportamento resultante: Necessidade de estruturar o projeto Android com Gradle Wrapper oficial, version catalog (`libs.versions.toml`), scripts de build Kotlin DSL e manifesto com namespace e package corretos (`com.playertwo1.rin`).
- Arquivos criados/alterados e motivo:
  - `gradle/wrapper/gradle-wrapper.properties` [NEW]: Gradle 8.11.1 fixado.
  - `gradlew`, `gradlew.bat` e `gradle-wrapper.jar` [NEW]: Binários do wrapper oficial.
  - `gradle/libs.versions.toml` [NEW]: Version catalog com todas as versões fixadas na ADR 0002.
  - `settings.gradle.kts` [NEW]: Configuração de repositórios e módulo `:app`.
  - `build.gradle.kts` [NEW]: Script raiz de plugins.
  - `app/build.gradle.kts` [NEW]: Configurações do módulo `:app`, compileSdk 35, minSdk 26, compose habilitado.
  - `app/src/main/AndroidManifest.xml` [NEW]: Manifesto mínimo com permissões e declaração de componentes.
  - `gradle.properties` [NEW]: Propriedades `android.useAndroidX=true` e configurações de JVM.
- Critérios da tarefa, resultado de cada um e evidência:
  1. Criar wrapper Gradle: ATENDIDO. Gradle 8.11.1 instalado e verificado via `.\gradlew.bat --version`.
  2. Catálogo de versões fixas: ATENDIDO. `libs.versions.toml` configurado e referenciado nos build scripts.
  3. Settings, app e configurações sem caminhos de máquina versionados: ATENDIDO. `local.properties` ignorado via `.gitignore`.
  4. Namespace/package coerente: ATENDIDO. `com.playertwo1.rin`.
- Decisões/ADRs afetadas: Segue fielmente [ADR 0002](../adr/0002-identidade-e-toolchain.md).
- Alterações anteriores do usuário preservadas: Sim, working tree íntegro.

## Validação executada

| Comando ou roteiro exato | Ambiente/revisão | Exit code/resultado observado | Artefato existente |
|---|---|---|---|
| `.\gradlew.bat --version` | PowerShell 7 / Windows | Exit 0; Gradle 8.11.1, JVM 17.0.20.1 | Wrapper instalado |
| `.\gradlew.bat help` | PowerShell 7 / Windows | Exit 0; BUILD SUCCESSFUL em 50s | Configuração do projeto |

- Testes não executados e motivo: Nenhum teste pendente para a configuração do build.
- Limites desta evidência: Valida a inicialização e avaliação da toolchain Gradle; código de UI e testes validados nas tarefas seguintes.

## Auditoria

- Tipo: autoauditoria segundo AUDIT.md e WATCHDOG.md
- Escopo, diff, regressão, qualidade, segurança, Git e caça a atalhos:
  - Nenhuma biblioteca desnecessária adicionada.
  - Caminho absoluto de SDK isolado em `local.properties` não commitado.
- AUDIT RESULT: PASS
- Achados: Nenhum.
- Riscos residuais: Nenhum.

## Continuidade

- PROJECT_STATE e CHANGELOG atualizados: Sim.
- Gate de fase: F01 em andamento.
- Próximo ID elegível: **F01-T02** — Criar shell de navegação.
- Bloqueios: Nenhum.
