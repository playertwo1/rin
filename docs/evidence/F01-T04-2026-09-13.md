# Entrega F01-T04 — Configurar CI e provar reprodutibilidade

## Identificação

- ID e título: F01-T04 — Configurar CI e provar reprodutibilidade
- Data e responsável: 2026-09-13, Antigravity (autorizado por Rafael)
- Estado: EM VALIDAÇÃO (workflow criado e validado localmente; execução remota no GitHub Actions pendente de autorização de push)
- Tipo de evidência: local / CI workflow
- HEAD base e alterações não commitadas: `cd49296` + código F01
- Pedido autorizado e dependências verificadas: Fase F01 autorizada; F01-T01, F01-T02 e F01-T03 concluídas

## Mudança e aceite

- Problema e comportamento resultante: Configuração da esteira de automação contínua (CI) no GitHub Actions para compilação, lint e testes unitários a cada push/pull request na branch `main`, garantindo reprodutibilidade do build em ambiente limpo sem dependência de caminhos de máquina local.
- Arquivos criados/alterados e motivo:
  - `.github/workflows/android-ci.yml` [NEW]: Pipeline automatizada com JDK 17 Temurin, cache Gradle, `lintDebug`, `testDebugUnitTest` e `assembleDebug`.
  - `docs/evidence/F01-T04-2026-09-13.md` [NEW]: Registro formal da entrega e limites de execução.
- Critérios da tarefa, resultado de cada um e evidência:
  1. Criar workflow para build, testes unitários e lint com versões compatíveis: ATENDIDO. Arquivo `.github/workflows/android-ci.yml` configurado com JDK 17 e Gradle actions oficiais.
  2. Executar workflow se houver autorização/acesso: REGISTRO HONESTO. Os comandos locais que compõem o workflow (`lintDebug`, `testDebugUnitTest`, `assembleDebug`) foram todos executados com exit 0 no ambiente local. Conforme guardrail mandatório de [AGENTS.md](../../AGENTS.md) e [WATCHDOG.md](../../AI_PROJECT_GUARDRAILS/WATCHDOG.md), nenhum push não autorizado foi realizado apenas para disparar a action na nuvem.
  3. Prova de reprodutibilidade: ATENDIDO localmente via Gradle Wrapper sem dependência de variáveis globais do sistema.
- Decisões/ADRs afetadas: Segue [ADR 0001](../adr/0001-plano-android-ate-1-0.md) e [ADR 0002](../adr/0002-identidade-e-toolchain.md).
- Alterações anteriores do usuário preservadas: Sim.

## Validação executada

| Comando ou roteiro exato | Ambiente/revisão | Exit code/resultado observado | Artefato existente |
|---|---|---|---|
| `.\gradlew.bat lintDebug` | Local / Windows / JDK 17 | Exit 0 | Relatório HTML em `app/build/reports/` |
| `.\gradlew.bat testDebugUnitTest` | Local / Windows / JDK 17 | Exit 0; 2 testes unitários passaram | Relatório em `app/build/reports/tests/` |
| `.\gradlew.bat assembleDebug` | Local / Windows / JDK 17 | Exit 0; APK gerado | `app-debug.apk` |
| Validação de sintaxe YAML | Parser YAML do workflow | Estrutura e etapas validadas | `.github/workflows/android-ci.yml` |

- Testes não executados e motivo: Execução remota no GitHub Actions NÃO EXECUTADA porque o repositório não recebeu push com as alterações da tarefa (respeito aos limites de segurança e guardrails).
- Limites desta evidência: A esteira está pronta para rodar assim que houver commit e push pelo mantenedor.

## Auditoria

- Tipo: autoauditoria segundo AUDIT.md e WATCHDOG.md
- Escopo, diff, regressão, qualidade, segurança, Git e caça a atalhos:
  - Não foi inventada execução remota inexistente de CI.
  - O YAML segue as versões exatas fixadas para o projeto.
- AUDIT RESULT: PASS
- Achados: Nenhum.
- Riscos residuais: Execução no GitHub Actions requer runners públicos com tempo hábil para download de dependências do Android.

## Continuidade

- PROJECT_STATE e CHANGELOG atualizados: Sim.
- Gate de fase: F01 com tarefas locais concluídas e comprovadas; trabalho autorizado avança para **F02** (Contrato móvel e gateway determinístico).
- Próximo ID elegível: **F02-T01** — Especificar contrato candidato executável.
- Bloqueios: Nenhum bloqueio para o desenvolvimento do fake determinístico na F02.
